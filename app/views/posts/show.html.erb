<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @post.description %>
</p>

<p>
  <strong>Categorie:</strong>
  <% if @post.categories.empty?%>
    <strong> Pas de catégorie</strong>
  <%else%>
  <%= @post.categories.map(&:name).join(',' )%>
  <%end%>
</p>

<p>
  <% if @post.comments.empty?%>
  <strong><%= @post.comments.count %> Commentaire</strong>
  <%else%>
  <strong> <%= @post.comments.name %> : <%=@post.comments.content%> </strong>
  <%end%>
</p>

<%= link_to "Ajouter un commentaire", new_comment_path %> |






<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
